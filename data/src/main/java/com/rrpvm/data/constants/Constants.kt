package com.rrpvm.data.constants

import java.text.SimpleDateFormat
import java.util.Locale

object Constants {
    val baseBackendDataFormat = SimpleDateFormat("dd/MM/yyyy HH:mm::ss", Locale.ENGLISH)
}